<h1>看板頁</h1>
<%# <div id="app">
  <App></App>
</div>
<div id="board">
  <Board></Board>
</div> %>

<%= link_to '新增看板', new_board_path, class: "abc", id: "xyz" if policy(:board).create?%>
<ul>
  <% @boards.each do |board| %>
    <li>
      <%= link_to board.title,board_path(board) %>
      <%= link_to '編輯', edit_board_path(board) %>
      <%= link_to '刪除', board_path(board),method: 'DELETE',data: {confirm: "Are you sure?"}%>
      <%= link_to '隱版', hide_board_path(board),method:'put'%>
    </li>
  <% end %>
</ul>
  <%= paginate @boards %>
  <%= paginate @boards,window:1 %>
